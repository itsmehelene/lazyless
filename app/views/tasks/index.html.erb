<h2>Bienvenue sur Lazyless</h2>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.name, task_path(task) %></li>
  <% end %>
</ul>

<ul>
  <li><%= link_to "Add a task", new_task_path(@task) %></li>
</ul>
